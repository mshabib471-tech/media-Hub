'use client';
import { useState } from 'react';
import { auth } from '@/lib/firebase';
import { signInWithEmailAndPassword } from 'firebase/auth';
export default function Admin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [user,setUser]=useState(auth.currentUser);async function login(){const r=await signInWithEmailAndPassword(auth,email,password);setUser(r.user)}if(!user)return <main className="grid min-h-screen place-items-center bg-slate-50 p-4"><section className="card w-full max-w-md p-8"><h1 className="text-3xl font-black">Admin Login</h1><input className="input mt-5" placeholder="Email" onChange={e=>setEmail(e.target.value)}/><input className="input mt-3" type="password" placeholder="Password" onChange={e=>setPassword(e.target.value)}/><button className="btn blue mt-4 w-full justify-center" onClick={login}>Secure Login</button></section></main>;return <main className="min-h-screen bg-slate-50 p-6"><h1 className="text-4xl font-black">Dashboard</h1><div className="mt-6 grid grid-cols-3 gap-4 grid3">{['Bookings','Services','Blog','Customers','Reviews','Analytics'].map(x=><div className="card p-6" key={x}><b className="text-2xl">{x}</b><p className="text-slate-500">Manage {x.toLowerCase()} with Firebase Firestore collections.</p></div>)}</div></main>}
